# Agent instructions

## Project skills

Project workflows live in `.agents/skills`; load one only when its description matches the task.

## Related repositories

AWS IAM for this site is managed in the sibling repository `../don-personal-iam`. For deployment
permission errors, load `expand-iam-permissions`; request access to the sibling repository if it
is outside the active workspace.
