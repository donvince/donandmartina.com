# Hugo build output
public/
resources/_gen/
.hugo_build.lock

# Node / Lambda
node_modules/
infra/lambda/**/config.js
infra/lambda/**/*.zip
# The bundle steps zip into the repo root, not next to the sources.
/pre-signup.zip
/auth-edge.zip

# Local
.DS_Store
.superpowers/
